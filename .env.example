# VoiceBillRecord — 环境变量模板（可提交仓库）
#
# 使用：复制为 .env（勿提交 .env）
#   cp .env.example .env
# 仅在「运行 Serverless / 本地 API」的机器或进程里加载；静态前端构建不要注入下列密钥。

# ------------------------------------------------------------------
# 仅服务端（Neon / Vercel Functions / 本地 node）
# ------------------------------------------------------------------

# Neon Postgres 连接串（仅此一处；禁止写进前端 HTML/JS）
DATABASE_URL=postgresql://USER:PASSWORD@ep-example.neon.tech/neondb?sslmode=require

# DeepSeek（仅 /api/parse 读取；官方 OpenAI 兼容 Chat API）
# 文档：https://api-docs.deepseek.com/
# base_url 可为 https://api.deepseek.com 或 https://api.deepseek.com/v1
DEEPSEEK_API_URL=https://api.deepseek.com
DEEPSEEK_API_KEY=replace-with-deepseek-key-never-in-browser
# 可选：deepseek-chat（默认）或 deepseek-reasoner 等
# DEEPSEEK_MODEL=deepseek-chat
# 可选：请求超时毫秒（默认 25000）
# DEEPSEEK_TIMEOUT_MS=25000

# ------------------------------------------------------------------
# HTTP 与安全边界
# ------------------------------------------------------------------

# 浏览器 Origin 白名单（跨域部署时必填）。GitHub Pages 静态 + Vercel API 时填 Pages 地址，多个逗号分隔。
CORS_ORIGIN=http://localhost:3000

# 可选：保护 /api/parse 与 /api/expenses 的简单 Bearer（与前端「可选设置」一致）
# API_GATE_TOKEN=replace-with-long-random-string

# ------------------------------------------------------------------
# 生产部署检查（E3）
# ------------------------------------------------------------------
# - Vercel（或同类）环境变量中设置：DATABASE_URL、DEEPSEEK_API_KEY、DEEPSEEK_API_URL（可选）、DEEPSEEK_MODEL（可选）、CORS_ORIGIN（Pages 域名）、API_GATE_TOKEN（可选）
# - GitHub Pages 仅托管静态文件时：前端请求的 API 基址须指向 Serverless 域名，并在该 API 环境配置 CORS_ORIGIN 为 Pages 源；密钥仍只放在 API 侧

# ------------------------------------------------------------------
# 说明：前端为 public/ 原生静态页，不经 Vite 打包
# ------------------------------------------------------------------
# 不要使用 VITE_API_BASE_URL 等 VITE_* 期望注入浏览器——本仓库不会读取。
# Pages 默认 API 根请写在 public/index.html 的 window.VBR_API_BASE_URL，或在网页「可选设置」里保存（localStorage）。

# ------------------------------------------------------------------
# 运行环境
# ------------------------------------------------------------------

NODE_ENV=development
