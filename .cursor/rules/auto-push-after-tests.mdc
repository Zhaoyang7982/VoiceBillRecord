---
description: VoiceBillRecord — 测试通过后提交并推送到远程主干
alwaysApply: true
---

# Git 提交与推送

在本仓库（VoiceBillRecord）中完成代码修改且 **`npm test` 全部通过**后：

1. 执行 `git status` 确认待提交文件；**不要** `git add` `.env` 或任何含真实密钥的文件。
2. `git add` 相关改动，`git commit -m "…"`（信息简短、可读）。
3. `git push origin main`（本仓库默认主干为 **main**；若用户远程仅使用 **master**，则 `git push origin main:master` 或按其约定分支推送）。

若用户在本轮对话中明确要求**不要提交或不要推送**，则跳过上述步骤。
