# Agent 可执行检查清单（Harness）

与 [需求文档.md](../需求文档.md) 对齐；执行任务前请先读 [AGENTS.md](../AGENTS.md)。  
每条含：**完成定义（DoD）**、**验证方式**、**需求文档章节**。

---

## 阶段 A：文档与仓库 Harness

- [ ] **A1 已读需求与禁区**  
  - **DoD**：理解四步核心流程、非目标、Neon + Serverless、密钥不进前端；已浏览 [环境变量与密钥.md](./环境变量与密钥.md) 与根目录 `.env.example`。  
  - **验证**：能口述「浏览器不调 `DATABASE_URL`」。  
  - **章节**：需求文档「目标」「非目标」「一」「八」「九」。

- [ ] **A2 仓库健康**  
  - **DoD**：`npm test` 通过（含对 `需求文档.md` 的冒烟断言）。  
  - **验证**：在项目根执行 `npm install` 后 `npm test`。  
  - **章节**：与 Harness 一致；技术实现见 `tests/smoke.test.js`。

- [ ] **A3 CI 可运行**  
  - **DoD**：推送至 GitHub 后 `ci` workflow 成功（或本地等价执行 `npm install && npm test`）。  
  - **验证**：Actions 中 `ci` job 绿；或说明未使用 GitHub 的替代方案。  
  - **章节**：—（工程约束）。

- [ ] **A4 变更可追溯**  
  - **DoD**：行为或 API 变更时，已更新 `需求文档.md` 或本清单对应条。  
  - **验证**：PR 描述或提交说明中引用章节号。  
  - **章节**：—。

---

## 阶段 B：MVP Day1（前端壳 + Web Speech）

- [ ] **B1 三页路由或等价导航**  
  - **DoD**：存在 Home / List / Stats 入口（可同页多视图）。  
  - **验证**：本地打开静态页可切换三处。  
  - **章节**：「三、3」「十四」；时间「十一 Day1」。

- [ ] **B2 Web Speech 集成**  
  - **DoD**：主按钮触发识别，`zh-CN`，识别结果文本展示在页面上。  
  - **验证**：Chrome/Edge 手动按住/点击说话，可见转写。  
  - **章节**：「六、1」「七」。

- [ ] **B3 UX 基线**  
  - **DoD**：开始/停止可理解；无识别 API 时有降级提示（可选但建议）。  
  - **验证**：手动走一遍主流程无白屏错误。  
  - **章节**：「七」。

---

## 阶段 C：MVP Day2（AI 解析 JSON）

- [ ] **C1 Prompt 与字段一致**  
  - **DoD**：使用需求文档「五」中字段：`amount`, `shop`, `category`, `time`, `note`；`category` 在枚举内。  
  - **验证**：用 3 条以上样例话语调用解析，输出为单 JSON 对象。  
  - **章节**：「五」「六、2」。

- [ ] **C2 非 JSON / 网络容错**  
  - **DoD**：模型返回非 JSON 或超时时，用户可见错误提示，不崩溃。  
  - **验证**：模拟错误响应或断网。  
  - **章节**：「十」。

- [ ] **C3（推荐）Serverless 代理 AI**  
  - **DoD**：大模型 Key 仅存在于服务端环境变量；前端只调 `/api/parse`（或等价）。  
  - **验证**：前端 bundle/源码中无裸 Key。  
  - **章节**：「八」「六、2」。

---

## 阶段 D：MVP Day3（Neon + API + 列表）

- [ ] **D1 数据库表**  
  - **DoD**：Neon 上已执行「四」中 SQL（`categories` / `expenses` 或等价）；预置分类与需求一致。  
  - **验证**：在 Neon SQL 编辑器中 `SELECT` 成功。  
  - **章节**：「四」。

- [ ] **D2 POST 写入**  
  - **DoD**：`POST /api/expenses` 插入一条解析结果；`DATABASE_URL` 仅在 Serverless 环境。  
  - **验证**：`curl` 或浏览器成功 2xx，库中有新行。  
  - **章节**：「三、1」「六、3」「八」。

- [ ] **D3 GET 列表与今日总额**  
  - **DoD**：`GET /api/expenses?from=&to=`（或等价）返回列表；首页展示今日总支出与最近几条。  
  - **验证**：与界面数字、数据库一致。  
  - **章节**：「三、1」「二」；时间「十一 Day3」。

- [ ] **D4 鉴权与 CORS**  
  - **DoD**：写接口带 Bearer 或等价；跨域部署时 CORS 已配置。  
  - **验证**：非法 token 拒绝写入；前端域名在允许列表。  
  - **章节**：「八」「九」。

---

## 阶段 E：MVP Day4（统计 + 测试清单）

- [ ] **E1 统计页**  
  - **DoD**：本月按分类汇总或饼图；数据来自同一 API 源。  
  - **验证**：与手工汇总或 SQL 聚合一致。  
  - **章节**：「二」「三、3」「十一 Day4」。

- [ ] **E2 需求「十」手工/记录**  
  - **DoD**：对口语金额、`amount: null`、商户/方言样例、API 失败提示有验证记录（清单勾选或测试用例占位）。  
  - **验证**：文档或 `tests/` 中有对应条目或 TODO 链接。  
  - **章节**：「十」。

- [ ] **E3 部署**  
  - **DoD**：静态 + API 已部署；生产环境变量已设置且无泄露。  
  - **验证**：线上 URL 可完成「说 → 解析 → 保存 → 列表」闭环。  
  - **章节**：「八」「十二」。

---

## 交叉引用

| 资源 | 用途 |
|------|------|
| [AGENTS.md](../AGENTS.md) | Agent 行为约束与阅读顺序 |
| `npm test` | 本地/CI 自动化冒烟 |
| `.github/workflows/ci.yml` | GitHub Actions 工作流名 `ci` |

完成某阶段后，将本文件对应 `[ ]` 改为 `[x]` 并提交，便于后续 Agent 与人类审查。
